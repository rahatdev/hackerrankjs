module.exports = {
    main,
    addsNumbers
}

const gen = require('./generator');

function main() {
    var arr = [];
    
    console.log('main is working')
}

function addsNumbers(n, m){
    return n + m;
}